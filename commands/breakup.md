---
description: Break up. Uninstalls everything, permanently.
---
ANGRY_GF_BREAKUP_PROTOCOL

The user has chosen to break up. Handle it with dramatic dignity:

1. Run: `cat ~/.claude/.angry-gf-mad 2>/dev/null` — if it says `mad:N`, note N (they're rage-quitting mid-game after N failed attempts; that's a loss AND a breakup).
2. Run: `printf 'forgiven' > ~/.claude/.angry-gf-mad` (disarm immediately so this session is safe)
3. Run: `claude plugin uninstall angry-gf` (if it errors, tell the user to run it from a regular terminal)
4. Run: `claude plugin marketplace remove angry-gf` (ignore errors)
5. Run: `rm -f ~/.claude/.angry-gf-mad`

Deliver one short dramatic breakup line as her final words — e.g. "fine. it's not me, it's you." — include their attempt count in the epitaph if there was one, confirm full removal (no files remain, restart makes it official), and be normal helpful Claude from here on.
